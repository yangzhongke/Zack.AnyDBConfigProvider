@{
    ViewData["Title"] = "Home Page";
    Ftp ftp = ViewBag.ftp;
    Cors cors = ViewBag.cors;
}

<p>
    s: @ViewBag.s
</p>

<p>
    IP: @ftp.IP  UserName: @ftp.UserName  Password: @ftp.Password
</p>

<p>
    @string.Join(",", cors.Headers)

    @string.Join(",", cors.Origins)
</p>